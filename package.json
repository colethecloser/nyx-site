{
  "name": "nyx-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/migrate.js --seed",
    "db:demo": "node scripts/migrate.js --demo",
    "demo": "npm run db:demo && node scripts/demo-login.js --all && next dev",
    "doctor": "node scripts/doctor.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "next": "^14.2.35",
    "pg": "^8.22.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "resend": "^6.18.1",
    "stripe": "^22.4.0",
    "zod": "^4.4.3"
  }
}
